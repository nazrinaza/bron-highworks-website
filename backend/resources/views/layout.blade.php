<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>@yield('title', 'BRON Operations') | BRON Highworks</title><link rel="icon" href="{{ asset('brand/favicon.svg') }}"><link rel="stylesheet" href="{{ asset('operations.css').'?v='.substr(hash_file('sha256', public_path('operations.css')), 0, 12) }}"></head><body>
<header class="topbar"><a href="{{ route('home') }}"><img src="{{ asset('brand/assets/bron-logo.png') }}" width="155" height="50" alt="BRON Highworks"></a><span>HIGH ACCESS. SERIOUS CLEANING.</span>@auth<form method="post" action="{{ route('logout') }}">@csrf<button class="outline">Sign out</button></form>@else<a href="{{ route('home') }}">Back to website ↗</a>@endauth</header>
<div class="shell">@auth<aside><p class="eyebrow">OPERATIONS</p><a href="{{ route('admin.visits.index') }}" @class(['active'=>request()->routeIs('admin.visits.*')])>Site visits</a>@foreach(\App\Models\BusinessDocument::TYPES as $key=>$label)<a href="{{ route('admin.documents.index',['type'=>$key]) }}" @class(['active'=>request('type')===$key])>{{ $label }}s</a>@endforeach<div class="aside-bottom">BRON HIGHWORKS<br><small>Kuala Lumpur, Malaysia</small></div></aside>@endauth
<main><div class="page-heading"><p class="eyebrow">BRON HIGHWORKS</p><h1>@yield('title')</h1>@yield('intro')</div>
@if(session('success'))<div class="notice" role="status">{{ session('success') }}</div>@endif
@if($errors->any())<div class="errors" role="alert"><strong>Please check the following:</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
@yield('content')</main></div><script src="{{ asset('operations.js') }}" defer></script></body></html>